{/* {searchInputs.searchItem.length > 0 ? (
                            selectedFile.filter((item, index) => {
                                if (item.imgName.includes(searchInputs.searchItem)) {
                                    return (
                                        array.map((item,index)=>(
                                            <div className={classes.imageBox} key={index}>
                                                <img src={item.baseData} alt="Uploaded" />
                                                <p style={{ fontSize: '10px', display: 'flex', textAlign: 'center' }}>
                                                {item.imgName}
                                                </p>
                                            </div>
                                        ))
                                        
                                    );
                                }
                                return null; 
                                })
                            ) : (
                            <div className={classes.images}>
                                {selectedFile.map((selectFile, index) => (
                                <div key={index} className={classes.imageBox}>
                                    <img src={selectFile.baseData} alt="Uploaded" />
                                    <p style={{ fontSize: '10px', display: 'flex', textAlign: 'center' }}>
                                    {selectFile.imgName}
                                    </p>
                                </div>
                                ))}
                            </div>
                            )} */}
                        {/* {console.log(searchInputs.searchItem.length)} */}

                    